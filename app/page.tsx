import { Login } from "@/app/(features)/containers";

export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-muted">
      <Login />
    </div>
  );
}
